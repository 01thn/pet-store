spring:
  application:
    name: private-service
  cloud:
    config:
      uri: ${CLOUD_URI:http://localhost:8888}
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user